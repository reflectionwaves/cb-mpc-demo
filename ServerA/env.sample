# ====== cb-mpc demo environment (v2 with OpenSSL) ======
# Copy this file to 'env' and edit as needed.

# Role of this server: A or B
ROLE="A"

# Networking
SELF_HOST="0.0.0.0"
SELF_PORT="7001"
PEER_HOST="CHANGEME"            # e.g., 10.0.0.2 or server-b.example.com
PEER_PORT="7002"

# Versions and toggles
OPENSSL_VER="3.2.0"
BUILD_STATIC_OPENSSL="${BUILD_STATIC_OPENSSL:-yes}"   # yes/no
LLVM_VERSION="${LLVM_VERSION:-20}"

# Directories
CBMPC_DIR="${CBMPC_DIR:-$HOME/cb-mpc}"
DEMO_DIR="${DEMO_DIR:-$HOME/cb-mpc-demo}"

# Build / install preferences
USE_SUDO_INSTALL="${USE_SUDO_INSTALL:-yes}"
PARALLEL_JOBS="${PARALLEL_JOBS:-$(nproc)}"

# cb-mpc install prefix and CMake hints
CBMPC_PREFIX="/usr/local/opt/cbmpc"
CMAKE_PREFIX_PATH="${CBMPC_PREFIX}"
CGO_CFLAGS="-I${CBMPC_PREFIX}/include"
CGO_LDFLAGS="-L${CBMPC_PREFIX}/lib -lcbmpc -lssl -lcrypto -lm -ldl -lpthread"

# OpenSSL prefix (used by scripts; avoid editing unless you know you need to)
OPENSSL_PREFIX="/usr/local/opt/openssl@${OPENSSL_VER}"
